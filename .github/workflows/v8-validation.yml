name: V8 XAUUSD M5 Validation
on:
  workflow_dispatch:
  push:
    paths:
      - 'strategy_v8.js'
      - 'backtest/v8_backtest.js'
      - 'backtest/v8_lookahead_audit.js'
      - '.github/workflows/v8-validation.yml'
jobs:
  validate:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 20
      - run: npm install --no-audit --no-fund dukascopy-node@1.50.0
      - name: Backtest
        run: node backtest/v8_backtest.js
      - name: Causal audit
        run: node backtest/v8_lookahead_audit.js
      - uses: actions/upload-artifact@v4
        with:
          name: v8-validation
          path: backtest/v8_result.json
      - name: Persist result
        run: |
          git config user.name 'gold-alert-bot'
          git config user.email 'actions@github.com'
          git add backtest/v8_result.json
          git diff --cached --quiet || git commit -m 'persist V8 validation result [skip ci]'
          git push